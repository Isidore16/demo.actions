name: 2 Hello World

on:
  workflow_dispatch:
env:
  APP_ENV: dev  

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Build code
        run: echo "hello your address is ${{ vars.APP.ENV}}"
