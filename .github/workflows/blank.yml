name: 2 Hello World
on:
  push:
   branches:
    - main
    - develop
  pull_request:
   branches:
    - main
    - testpull
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Build code
        run: echo "Building code..."

