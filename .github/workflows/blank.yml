name: 2 Hello World

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Build code
        run: echo "hello your address is ${{ ADRESS }}"
