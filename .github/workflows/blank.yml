name: "ENV World"

on:
  workflow_dispatch:

env:
  APP_ENV: dev  

jobs:
  ved-demo:
    env:
      APP_ENV: test
    runs-on: ubuntu-latest
    steps:
      - name: connect to ENV
     env:
       APP_ENV: prod
    run: echo "connected to $APP_ENV"
